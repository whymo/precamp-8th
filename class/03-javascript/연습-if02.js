nst profile = {
    name : "철수",
    age : 12,
    school : "다람쥐초등학교" }
    undefined
    if (profile.age >= 20) {
    console.log("성인입니다!")
    } else if (profile.age >=8) {
    console.log("학생입니다!")
    } else {
    console.log("어린이입니다!")
    }
    VM1890:4 학생입니다!
    undefined
    // if문에 조건을 먼저 적는데 그때는 ()를 이용하여 작성하고, 그 후에 {}를 활용하여 각 조건의 결과값을 적는다. if - else if - else (else는 생략이 가능하다.)